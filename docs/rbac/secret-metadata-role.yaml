# Optional v0.5 Secret-metadata fixture. RBAC cannot limit a Secret response to metadata.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kupilot-secret-metadata-read
  namespace: example-namespace
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
