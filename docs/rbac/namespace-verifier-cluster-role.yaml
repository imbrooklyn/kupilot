# Current v0.1.0 exact Namespace-verification helper.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kupilot-namespace-verifier
rules:
  - apiGroups: [""]
    resources: ["namespaces"]
    resourceNames: ["example-namespace"]
    verbs: ["get"]
